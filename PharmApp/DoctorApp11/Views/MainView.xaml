<Window x:Class="DoctorApp11.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="600" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="0" Grid.RowSpan="3" prism:RegionManager.RegionName="ContentRegion" />
        <ContentControl Grid.Row="0" Grid.RowSpan="3" prism:RegionManager.RegionName="Menu"/>
        <ContentControl Grid.Row="0" Grid.RowSpan="3" prism:RegionManager.RegionName="Hello"/>
        <ContentControl Grid.Row="1" Grid.RowSpan="2" prism:RegionManager.RegionName="ListV"/>
        <ContentControl Grid.Row="0" Grid.RowSpan="2" prism:RegionManager.RegionName="Find"/>
        <ContentControl Grid.Row="2" prism:RegionManager.RegionName="Reset"/>
        
    </Grid>
</Window>
